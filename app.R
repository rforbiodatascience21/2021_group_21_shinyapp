library(shiny)

ui <- fluidPage(
  titlePanel(""),
  sidebarLayout(
    
  ),
  mainPanel(
    
  )
)

server <- function(input, output) {
  
  
  
}

# Create Shiny app ----
shinyApp(ui = ui, server = server)